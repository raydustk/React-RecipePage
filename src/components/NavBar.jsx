import React from 'react';

const NavBar = () => {
    return (
        <nav className="navbar">
            <h1 className="navbar-heading">A m b r o s i a</h1>
        </nav>
    );
};

export default NavBar;